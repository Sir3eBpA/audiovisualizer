import { styled } from "@mui/material/styles";
import { ImageList } from "@mui/material";

export const PresetsCardList = styled(ImageList)({
  width: "100%",
  height: "100%",
  justifyItems: "center",
});
