import { styled } from "@mui/material/styles";
import { Box} from "@mui/material";

export interface SpinnerBoxSettings {
  display?: string,
  justifyContent?: string,
  margin?: number|string,
}

export const SpinnerBox = styled(Box)({

});
